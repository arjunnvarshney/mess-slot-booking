NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/mess-booking?replicaSet=rs0
JWT_SECRET=
CORS_ORIGINS=http://localhost:5173,http://localhost:4173
# Set only when your host has exactly this many trusted reverse proxy hops:
# TRUST_PROXY_HOPS=1
# Provisioning scripts only; do not commit real values:
# ADMIN_USERNAME=
# ADMIN_PASSWORD=
# ALLOW_DEMO_SEED=true
# DEMO_STUDENT_PASSWORD=
