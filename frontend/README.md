# Frontend

This is the single supported React application for both students and administrators. See the root README for setup, deployment, and verification.

Run `npm ci`, then `npm run dev`. The development proxy forwards /api to localhost:5000. Never put backend secrets in VITE_ variables.
